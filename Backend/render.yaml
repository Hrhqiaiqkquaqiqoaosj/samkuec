services:
  - type: web
    name: samku-cms-backend
    env: node
    buildCommand: npm cache clean --force && npm install
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /health